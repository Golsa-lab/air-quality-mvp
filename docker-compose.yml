services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aq
      POSTGRES_PASSWORD: aq
      POSTGRES_DB: airquality
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
